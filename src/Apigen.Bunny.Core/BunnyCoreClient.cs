//
// <auto-generated>
// Bunny Core API Client - Auto-generated from OpenAPI specification
//
// Generated by: ApiGenerator
// Source: specs/core.json
// API: bunny.net API
// Version: 1.0.0
//
// Changes to this file may cause incorrect behavior and will be lost if
// the code is regenerated.
// </auto-generated>
//
using System;
using System.Net.Http;
using System.Text.Json;
using System.Text.Json.Serialization;
using Apigen.Bunny.Core.Models;
using Microsoft.Extensions.Logging;

#nullable enable

namespace Apigen.Bunny.Core;

/// <summary>
/// Main API client for accessing all resources
/// </summary>
public partial class BunnyCoreClient : IDisposable
{
  private readonly HttpClient _httpClient;
  private readonly bool _disposeHttpClient;
  private readonly ILogger? _logger;

  /// <summary>
  /// Client for Countries operations
  /// </summary>
  public CountriesClient Countries { get; }

  /// <summary>
  /// Client for DNS Zone operations
  /// </summary>
  public DnsZoneClient DnsZone { get; }

  /// <summary>
  /// Client for Pull Zone operations
  /// </summary>
  public PullZoneClient PullZone { get; }

  /// <summary>
  /// Client for Purge operations
  /// </summary>
  public PurgeClient Purge { get; }

  /// <summary>
  /// Client for Region operations
  /// </summary>
  public RegionClient Region { get; }

  /// <summary>
  /// Client for Storage Zone operations
  /// </summary>
  public StorageZoneClient StorageZone { get; }

  /// <summary>
  /// Client for Stream Video Library operations
  /// </summary>
  public StreamVideoLibraryClient StreamVideoLibrary { get; }

  /// <summary>
  /// Client for User operations
  /// </summary>
  public UserClient User { get; }

  /// <summary>
  /// Client for AuditLog operations
  /// </summary>
  public AuditLogClient AuditLog { get; }

  /// <summary>
  /// Client for Statistics operations
  /// </summary>
  public StatisticsClient Statistics { get; }

  /// <summary>
  /// Client for Search operations
  /// </summary>
  public SearchClient Search { get; }

  /// <summary>
  /// Client for Pricing operations
  /// </summary>
  public PricingClient Pricing { get; }

  /// <summary>
  /// Client for Load Balancer operations
  /// </summary>
  public LoadBalancerClient LoadBalancer { get; }

  /// <summary>
  /// Client for Billing operations
  /// </summary>
  public BillingClient Billing { get; }

  /// <summary>
  /// Client for API Keys operations
  /// </summary>
  public ApiKeysClient ApiKeys { get; }

  /// <summary>
  /// Client for Affiliate operations
  /// </summary>
  public AffiliateClient Affiliate { get; }

  /// <summary>
  /// Initialize client with a pre-configured HttpClient
  /// </summary>
  /// <param name="httpClient">Pre-configured HttpClient with base address, auth headers, etc.</param>
  /// <param name="logger">Optional logger for request/response logging</param>
  public BunnyCoreClient(HttpClient httpClient, ILogger? logger = null)
  {
    _httpClient = httpClient;
    _disposeHttpClient = false;
    _logger = logger;

    Countries = new CountriesClient(_httpClient, _logger);
    DnsZone = new DnsZoneClient(_httpClient, _logger);
    PullZone = new PullZoneClient(_httpClient, _logger);
    Purge = new PurgeClient(_httpClient, _logger);
    Region = new RegionClient(_httpClient, _logger);
    StorageZone = new StorageZoneClient(_httpClient, _logger);
    StreamVideoLibrary = new StreamVideoLibraryClient(_httpClient, _logger);
    User = new UserClient(_httpClient, _logger);
    AuditLog = new AuditLogClient(_httpClient, _logger);
    Statistics = new StatisticsClient(_httpClient, _logger);
    Search = new SearchClient(_httpClient, _logger);
    Pricing = new PricingClient(_httpClient, _logger);
    LoadBalancer = new LoadBalancerClient(_httpClient, _logger);
    Billing = new BillingClient(_httpClient, _logger);
    ApiKeys = new ApiKeysClient(_httpClient, _logger);
    Affiliate = new AffiliateClient(_httpClient, _logger);
  }

  private BunnyCoreClient(HttpClient httpClient, bool disposeHttpClient, ILogger? logger)
  {
    _httpClient = httpClient;
    _disposeHttpClient = disposeHttpClient;
    _logger = logger;

    Countries = new CountriesClient(_httpClient, _logger);
    DnsZone = new DnsZoneClient(_httpClient, _logger);
    PullZone = new PullZoneClient(_httpClient, _logger);
    Purge = new PurgeClient(_httpClient, _logger);
    Region = new RegionClient(_httpClient, _logger);
    StorageZone = new StorageZoneClient(_httpClient, _logger);
    StreamVideoLibrary = new StreamVideoLibraryClient(_httpClient, _logger);
    User = new UserClient(_httpClient, _logger);
    AuditLog = new AuditLogClient(_httpClient, _logger);
    Statistics = new StatisticsClient(_httpClient, _logger);
    Search = new SearchClient(_httpClient, _logger);
    Pricing = new PricingClient(_httpClient, _logger);
    LoadBalancer = new LoadBalancerClient(_httpClient, _logger);
    Billing = new BillingClient(_httpClient, _logger);
    ApiKeys = new ApiKeysClient(_httpClient, _logger);
    Affiliate = new AffiliateClient(_httpClient, _logger);
  }

  /// <summary>
  /// Create client with API key authentication
  /// </summary>
  public static BunnyCoreClient WithAccessKey(string accessKey, string baseUrl = "https://api.bunny.net", ILogger? logger = null)
  {
    HttpClient httpClient = CreateTokenAuthHttpClient(accessKey, baseUrl, "AccessKey", false);
    return new BunnyCoreClient(httpClient, true, logger);
  }

  /// <summary>
  /// Create client with API key authentication
  /// </summary>
  public static BunnyCoreClient WithBearer(string bearer, string baseUrl = "https://api.bunny.net", ILogger? logger = null)
  {
    HttpClient httpClient = CreateTokenAuthHttpClient(bearer, baseUrl, "Authorization", false);
    return new BunnyCoreClient(httpClient, true, logger);
  }

  private static HttpClient CreateTokenAuthHttpClient(string apiToken, string baseUrl, string headerName, bool useBearer)
  {
    // Ensure baseUrl ends with / for proper Uri combining with relative paths
    string normalizedBaseUrl = baseUrl.EndsWith("/") ? baseUrl : baseUrl + "/";
    HttpClient client = new() { BaseAddress = new Uri(normalizedBaseUrl) };

    if (useBearer)
    {
      client.DefaultRequestHeaders.Add(headerName, $"Bearer {apiToken}");
    }
    else
    {
      client.DefaultRequestHeaders.Add(headerName, apiToken);
    }

    return client;
  }

  private static HttpClient CreateBasicAuthHttpClient(string username, string password, string baseUrl)
  {
    // Ensure baseUrl ends with / for proper Uri combining with relative paths
    string normalizedBaseUrl = baseUrl.EndsWith("/") ? baseUrl : baseUrl + "/";
    HttpClient client = new() { BaseAddress = new Uri(normalizedBaseUrl) };

    string credentials = Convert.ToBase64String(System.Text.Encoding.UTF8.GetBytes($"{username}:{password}"));
    client.DefaultRequestHeaders.Add("Authorization", $"Basic {credentials}");

    return client;
  }

  private static HttpClient CreateCookieAuthHttpClient(string token, string cookieName, string baseUrl)
  {
    string normalizedBaseUrl = baseUrl.EndsWith("/") ? baseUrl : baseUrl + "/";
    System.Net.CookieContainer cookies = new();
    cookies.Add(new Uri(normalizedBaseUrl), new System.Net.Cookie(cookieName, token));
    HttpClientHandler handler = new() { CookieContainer = cookies };
    HttpClient client = new(handler) { BaseAddress = new Uri(normalizedBaseUrl) };

    return client;
  }

  /// <summary>
  /// Dispose resources
  /// </summary>
  public void Dispose()
  {
    if (_disposeHttpClient)
    {
      _httpClient?.Dispose();
    }
  }
}
