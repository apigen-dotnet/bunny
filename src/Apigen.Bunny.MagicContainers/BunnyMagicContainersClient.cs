//
// <auto-generated>
// Bunny MagicContainers API Client - Auto-generated from OpenAPI specification
//
// Generated by: ApiGenerator
// Source: specs/magic-containers.json
// API: Public API
// Version: 1.0
//
// Changes to this file may cause incorrect behavior and will be lost if
// the code is regenerated.
// </auto-generated>
//
using System;
using System.Net.Http;
using System.Text.Json;
using System.Text.Json.Serialization;
using Apigen.Bunny.MagicContainers.Models;
using Microsoft.Extensions.Logging;

#nullable enable

namespace Apigen.Bunny.MagicContainers;

/// <summary>
/// Main API client for accessing all resources
/// </summary>
public partial class BunnyMagicContainersClient : IDisposable
{
  private readonly HttpClient _httpClient;
  private readonly bool _disposeHttpClient;
  private readonly ILogger? _logger;

  /// <summary>
  /// Client for Applications operations
  /// </summary>
  public ApplicationsClient Applications { get; }

  /// <summary>
  /// Client for AutoscalingSettings operations
  /// </summary>
  public AutoscalingSettingsClient AutoscalingSettings { get; }

  /// <summary>
  /// Client for ContainerRegistries operations
  /// </summary>
  public ContainerRegistriesClient ContainerRegistries { get; }

  /// <summary>
  /// Client for Containers operations
  /// </summary>
  public ContainersClient Containers { get; }

  /// <summary>
  /// Client for Endpoints operations
  /// </summary>
  public EndpointsClient Endpoints { get; }

  /// <summary>
  /// Client for Limits operations
  /// </summary>
  public LimitsClient Limits { get; }

  /// <summary>
  /// Client for Nodes operations
  /// </summary>
  public NodesClient Nodes { get; }

  /// <summary>
  /// Client for Pods operations
  /// </summary>
  public PodsClient Pods { get; }

  /// <summary>
  /// Client for Regions operations
  /// </summary>
  public RegionsClient Regions { get; }

  /// <summary>
  /// Client for RegionSettings operations
  /// </summary>
  public RegionSettingsClient RegionSettings { get; }

  /// <summary>
  /// Client for Volumes operations
  /// </summary>
  public VolumesClient Volumes { get; }

  /// <summary>
  /// Client for Log Forwarding operations
  /// </summary>
  public LogForwardingClient LogForwarding { get; }

  /// <summary>
  /// Initialize client with a pre-configured HttpClient
  /// </summary>
  /// <param name="httpClient">Pre-configured HttpClient with base address, auth headers, etc.</param>
  /// <param name="logger">Optional logger for request/response logging</param>
  public BunnyMagicContainersClient(HttpClient httpClient, ILogger? logger = null)
  {
    _httpClient = httpClient;
    _disposeHttpClient = false;
    _logger = logger;

    Applications = new ApplicationsClient(_httpClient, _logger);
    AutoscalingSettings = new AutoscalingSettingsClient(_httpClient, _logger);
    ContainerRegistries = new ContainerRegistriesClient(_httpClient, _logger);
    Containers = new ContainersClient(_httpClient, _logger);
    Endpoints = new EndpointsClient(_httpClient, _logger);
    Limits = new LimitsClient(_httpClient, _logger);
    Nodes = new NodesClient(_httpClient, _logger);
    Pods = new PodsClient(_httpClient, _logger);
    Regions = new RegionsClient(_httpClient, _logger);
    RegionSettings = new RegionSettingsClient(_httpClient, _logger);
    Volumes = new VolumesClient(_httpClient, _logger);
    LogForwarding = new LogForwardingClient(_httpClient, _logger);
  }

  private BunnyMagicContainersClient(HttpClient httpClient, bool disposeHttpClient, ILogger? logger)
  {
    _httpClient = httpClient;
    _disposeHttpClient = disposeHttpClient;
    _logger = logger;

    Applications = new ApplicationsClient(_httpClient, _logger);
    AutoscalingSettings = new AutoscalingSettingsClient(_httpClient, _logger);
    ContainerRegistries = new ContainerRegistriesClient(_httpClient, _logger);
    Containers = new ContainersClient(_httpClient, _logger);
    Endpoints = new EndpointsClient(_httpClient, _logger);
    Limits = new LimitsClient(_httpClient, _logger);
    Nodes = new NodesClient(_httpClient, _logger);
    Pods = new PodsClient(_httpClient, _logger);
    Regions = new RegionsClient(_httpClient, _logger);
    RegionSettings = new RegionSettingsClient(_httpClient, _logger);
    Volumes = new VolumesClient(_httpClient, _logger);
    LogForwarding = new LogForwardingClient(_httpClient, _logger);
  }

  /// <summary>
  /// Create client with API key authentication
  /// </summary>
  public static BunnyMagicContainersClient WithApiKey(string apiKey, string baseUrl = "https://api.bunny.net/mc", ILogger? logger = null)
  {
    HttpClient httpClient = CreateTokenAuthHttpClient(apiKey, baseUrl, "AccessKey", false);
    return new BunnyMagicContainersClient(httpClient, true, logger);
  }

  private static HttpClient CreateTokenAuthHttpClient(string apiToken, string baseUrl, string headerName, bool useBearer)
  {
    // Ensure baseUrl ends with / for proper Uri combining with relative paths
    string normalizedBaseUrl = baseUrl.EndsWith("/") ? baseUrl : baseUrl + "/";
    HttpClient client = new() { BaseAddress = new Uri(normalizedBaseUrl) };

    if (useBearer)
    {
      client.DefaultRequestHeaders.Add(headerName, $"Bearer {apiToken}");
    }
    else
    {
      client.DefaultRequestHeaders.Add(headerName, apiToken);
    }

    return client;
  }

  private static HttpClient CreateBasicAuthHttpClient(string username, string password, string baseUrl)
  {
    // Ensure baseUrl ends with / for proper Uri combining with relative paths
    string normalizedBaseUrl = baseUrl.EndsWith("/") ? baseUrl : baseUrl + "/";
    HttpClient client = new() { BaseAddress = new Uri(normalizedBaseUrl) };

    string credentials = Convert.ToBase64String(System.Text.Encoding.UTF8.GetBytes($"{username}:{password}"));
    client.DefaultRequestHeaders.Add("Authorization", $"Basic {credentials}");

    return client;
  }

  private static HttpClient CreateCookieAuthHttpClient(string token, string cookieName, string baseUrl)
  {
    string normalizedBaseUrl = baseUrl.EndsWith("/") ? baseUrl : baseUrl + "/";
    System.Net.CookieContainer cookies = new();
    cookies.Add(new Uri(normalizedBaseUrl), new System.Net.Cookie(cookieName, token));
    HttpClientHandler handler = new() { CookieContainer = cookies };
    HttpClient client = new(handler) { BaseAddress = new Uri(normalizedBaseUrl) };

    return client;
  }

  /// <summary>
  /// Dispose resources
  /// </summary>
  public void Dispose()
  {
    if (_disposeHttpClient)
    {
      _httpClient?.Dispose();
    }
  }
}
